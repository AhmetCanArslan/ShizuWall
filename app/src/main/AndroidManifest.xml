<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- permission for querying all packages -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShizuWall"
        android:extractNativeLibs="false"
        tools:targetApi="31">
        <activity
            android:name="com.arslan.shizuwall.ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.arslan.shizuwall.ui.onboarding.OnboardingActivity" />
        <activity android:name="com.arslan.shizuwall.ui.shizuku.ShizukuSetupActivity" />
        <activity
            android:name="com.arslan.shizuwall.ui.settings.SettingsActivity"
            android:label="Settings"
            android:parentActivityName="com.arslan.shizuwall.ui.main.MainActivity"
            android:exported="false" />
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round" />

        <!-- Boot receiver: when device reboots, check saved firewall state and notify user to re-enable if needed -->
        <receiver
            android:name="com.arslan.shizuwall.receiver.BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.arslan.shizuwall.receiver.FirewallControlReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.arslan.shizuwall.ACTION_FIREWALL_CONTROL" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.arslan.shizuwall.service.FirewallTileService"
            android:label="Firewall Toggle"
            android:icon="@drawable/ic_quick_tile"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.arslan.shizuwall.widget.FirewallWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.arslan.shizuwall.ACTION_FIREWALL_STATE_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/firewall_widget_info" />
        </receiver>

        <!-- ...existing code... -->
    </application>

</manifest>